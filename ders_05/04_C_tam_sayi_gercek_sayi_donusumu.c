/* 
* C'de tam sayı gerçek sayı dönüşümüne başka bir örnek 
*/
#include <stdio.h>

int main(void)
{
    	int sum = 345;
	int cnt = 17;

	double result = (double)sum / cnt; //operandlardan biri double türüne dönüştürüldüğü için sonuç double olacaktır.
}
